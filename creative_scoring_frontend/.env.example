# PUBLIC_INTERFACE
# Frontend environment example file.
# Configure the Django backend API base path for the frontend.
# If the backend is proxied under the same domain, /api is fine.
REACT_APP_API_BASE=/api
